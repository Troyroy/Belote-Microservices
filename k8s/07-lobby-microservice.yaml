apiVersion: apps/v1
kind: Deployment
metadata:
  name: lobby-microservice
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lobby-microservice
  template:
    metadata:
      labels:
        app: lobby-microservice
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: lobby-microservice
          image: lobby-microservice:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: ADMIN_PASSWORD

---
apiVersion: v1
kind: Service
metadata:
  name: lobby-microservice
  namespace: microservices
spec:
  selector:
    app: lobby-microservice
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP